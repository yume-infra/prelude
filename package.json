{
  "name": "prelude-workspace",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules",
    "verify": "pnpm verify:code",
    "verify:code": "pnpm build && pnpm typecheck && pnpm -r --if-present test && pnpm lint --max-warnings 0 && pnpm knip",
    "acceptance:installed": "pnpm --filter @sayoriqwq/prelude-contract build && pnpm --filter @sayoriqwq/prelude build && node --experimental-strip-types apps/cli/tests/acceptance/installed-tracer.ts",
    "acceptance:packed-effect": "node --experimental-strip-types apps/cli/tests/acceptance/packed-effect-gate.ts",
    "deps:check": "taze -r",
    "deps:check:all": "taze -r --all",
    "deps:fresh": "taze minor -r -w -i --maturity-period 7",
    "outdated": "pnpm outdated -r",
    "deps": "pnpm update -r",
    "deps:latest": "pnpm update -r --latest",
    "build:cli": "pnpm --filter @sayoriqwq/prelude build",
    "link": "pnpm --filter @sayoriqwq/prelude link --global",
    "unlink": "pnpm --filter @sayoriqwq/prelude unlink --global",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm verify && pnpm acceptance:installed && changeset publish",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "prepare": "effect-tsgo patch",
    "knip": "knip --exclude duplicates --no-config-hints",
    "typecheck": "pnpm typecheck:root && pnpm -r --if-present typecheck",
    "typecheck:root": "node --experimental-strip-types tooling/run-tsgo.ts --noEmit --project tsconfig.json",
    "typecheck:tsc": "tsc -p tsconfig.json --noEmit && pnpm -r --if-present typecheck:tsc"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@changesets/cli": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "@effect/language-service": "catalog:",
    "@effect/tsgo": "catalog:",
    "@effect/vitest": "catalog:",
    "@sayoriqwq/effect-harness": "catalog:",
    "@sayoriqwq/prelude": "workspace:^",
    "@types/node": "catalog:",
    "@typescript/native": "catalog:",
    "eslint": "catalog:",
    "husky": "catalog:",
    "knip": "catalog:",
    "lint-staged": "catalog:",
    "taze": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
