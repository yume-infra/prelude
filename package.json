{
  "name": "prelude-workspace",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules",
    "verify": "pnpm --filter @sayoriqwq/prelude build && pnpm --filter @sayoriqwq/prelude test && pnpm lint && pnpm knip",
    "verify:code": "pnpm --filter @sayoriqwq/prelude build && pnpm --filter @sayoriqwq/prelude test && pnpm lint && pnpm knip",
    "smoke:dry-run": "pnpm --filter @sayoriqwq/prelude smoke:dry-run",
    "smoke:examples": "pnpm --filter @sayoriqwq/prelude smoke:examples",
    "deps:check": "taze -r",
    "deps:check:all": "taze -r --all",
    "deps:fresh": "taze minor -r -w -i --maturity-period 7",
    "outdated": "pnpm outdated -r",
    "deps": "pnpm update -r",
    "deps:latest": "pnpm update -r --latest",
    "build:cli": "pnpm --filter @sayoriqwq/prelude build",
    "link": "pnpm --filter @sayoriqwq/prelude link --global",
    "unlink": "pnpm --filter @sayoriqwq/prelude unlink --global",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm verify && pnpm smoke:dry-run && pnpm smoke:examples && changeset publish",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "prepare": "husky",
    "knip": "knip"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@changesets/cli": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "husky": "catalog:",
    "knip": "catalog:",
    "lint-staged": "catalog:",
    "taze": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:"
  }
}
