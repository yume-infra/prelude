# Create/Maintain Baseline Archive

This directory preserves the retired project-genesis plus provider-lifecycle architecture as historical evidence.

It was archived on 2026-07-10 after Prelude's target changed to multi-harness convergence. None of these files are current requirements, even where their body uses words such as `active`, `MUST`, `final`, or `source of truth`.

The former index is preserved as [`legacy-docs-index.md`](./legacy-docs-index.md). Current authority starts at [`../../README.md`](../../README.md).

