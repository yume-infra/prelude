---
status: accepted
date: 2026-07-12
amended: 2026-07-12
---

# V1 is proven by Effect Harness and Psychogram in Partita

Prelude V1 is not complete after a single real Harness or only synthetic multi-Harness fixtures. The real acceptance target is `/Users/sayori/Desktop/yume-infra/partita`, which must simultaneously select and run independent Effect Harness and Psychogram Artifacts.

Both Harnesses must export the shared Module contract. Prelude must load both from Partita's package graph, compose all outputs before writing, expose one Plan Document and execution hash, apply the approved initial integration, and execute composed verification. Effect and Psychogram repository changes are V1 work; Prelude must not preserve old provider compatibility to avoid them.

The Effect-plus-Psychogram Partita tracer defines the complete V1 product obligation. Prelude may delete legacy behavior, compatibility layers, generic output variants, lifecycle machinery, and platform breadth which that tracer and the frozen V1 contract do not require. V1 does not promise support for arbitrary third-party Harnesses, package managers, target languages, or old Prelude projects.

The proof includes two Artifact-managed static trees, shared root agent routing, real structured configuration where required, package requirement blockers, and one real Psychogram wiki under `psychogram/wikis/**`. The wiki demonstrates that the protocol is useful but remains target-owned and outside Prelude's desired-state model.

## Consequences

Automated fixtures still test determinism and conflicts, but cannot replace the Partita tracer. Final proof must not depend on sibling source-repository paths or mutable local discovery. Multi-wiki behavior, Psychogram instance management by Prelude, and Contract stabilization beyond what the two real Harnesses prove are outside the V1 acceptance scope.

This narrow proof scope does not authorize Effect- or Psychogram-specific branches in Prelude core. The host remains a domain-blind interpreter of the shared contract, but only the two first-party Modules receive V1 integration guarantees and release-gate coverage.
