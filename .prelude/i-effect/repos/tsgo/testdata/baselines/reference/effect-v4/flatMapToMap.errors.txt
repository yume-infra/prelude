=== Metadata ===
Effect version: 4.0.0

/.src/flatMapToMap.ts(5,3): suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
/.src/flatMapToMap.ts(11,3): suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
/.src/flatMapToMap.ts(17,71): suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
/.src/flatMapToMap.ts(22,39): suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
/.src/flatMapToMap.ts(25,38): suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)


==== /.src/flatMapToMap.ts (5 errors) ====
    import { Effect, pipe } from "effect"
    
    // Should trigger: pipeable style
    export const shouldTriggerPipeable = Effect.succeed(1).pipe(
      Effect.flatMap((n) => Effect.succeed(n + 1))
      ~~~~~~~~~~~~~~
!!! suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
    )
    
    // Should trigger: pipe style with a block body
    export const shouldTriggerPipe = pipe(
      Effect.succeed(1),
      Effect.flatMap((n) => {
      ~~~~~~~~~~~~~~
!!! suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
        return Effect.succeed(n + 1)
      })
    )
    
    // Should trigger: pipeable style with a function expression
    export const shouldTriggerFunctionExpression = Effect.succeed(1).pipe(Effect.flatMap(function(n) {
                                                                          ~~~~~~~~~~~~~~
!!! suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
      return Effect.succeed(n + 1)
    }))
    
    // Should trigger: data-first style
    export const shouldTriggerDataFirst = Effect.flatMap(Effect.succeed(1), (n) => Effect.succeed(n + 1))
                                          ~~~~~~~~~~~~~~
!!! suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
    
    // Should trigger: data-last style
    export const shouldTriggerDataLast = Effect.flatMap((n: number) => Effect.succeed(n + 1))(Effect.succeed(1))
                                         ~~~~~~~~~~~~~~
!!! suggestion TS377100: `Effect.map` expresses this success-value transformation more directly than `Effect.flatMap` followed by `Effect.succeed`. effect(flatMapToMap)
    
    // Should NOT trigger: callback returns another Effect operation
    export const shouldNotTriggerEffect = Effect.succeed(1).pipe(
      Effect.flatMap((n) => Effect.log(n))
    )
    
    // Should NOT trigger: callback contains additional logic
    export const shouldNotTriggerLogic = Effect.succeed(1).pipe(
      Effect.flatMap((n) => {
        console.log(n)
        return Effect.succeed(n + 1)
      })
    )
    
    // Should NOT trigger: unrelated flatMap and succeed functions
    const unrelated = {
      flatMap: <A, B>(value: A, f: (value: A) => B): B => f(value),
      succeed: <A>(value: A): A => value
    }
    export const shouldNotTriggerUnrelated = unrelated.flatMap(1, (n) => unrelated.succeed(n + 1))
    
