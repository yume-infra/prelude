=== Metadata ===
Effect version: 4.0.0

/.src/unnecessaryPipe_namespace.ts(8,29): suggestion TS377013: This pipe call contains no arguments. effect(unnecessaryPipe)


==== /.src/unnecessaryPipe_namespace.ts (1 errors) ====
    import * as Fn from "effect/Function"
    
    export const shouldNotReport = Fn.pipe(
      "Hello",
      (_) => _.length
    )
    
    export const shouldReport = Fn.pipe(
                                ~~~~~~~~
      "hello"
    ~~~~~~~~~
    )
    ~
!!! suggestion TS377013: This pipe call contains no arguments. effect(unnecessaryPipe)
    
