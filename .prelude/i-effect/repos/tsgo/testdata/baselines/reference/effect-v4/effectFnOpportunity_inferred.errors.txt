=== Metadata ===
Effect version: 4.0.0

/.src/effectFnOpportunity_inferred.ts(10,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("shouldHaveSuggestion")(function*(n) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_inferred.ts(17,17): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("shouldHaveSuggestionFunction")(function*(n) { ... })`. effect(effectFnOpportunity)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["inferred-span"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_inferred.ts (2 errors) ====
    import * as Effect from "effect/Effect"
    
    const _notExportedNoSuggestion = () => {
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return 42
      })
    }
    
    export const shouldHaveSuggestion = (n: number) => {
                 ~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("shouldHaveSuggestion")(function*(n) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return n
      })
    }
    
    export function shouldHaveSuggestionFunction(n: number) {
                    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("shouldHaveSuggestionFunction")(function*(n) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return n
      })
    }
    
