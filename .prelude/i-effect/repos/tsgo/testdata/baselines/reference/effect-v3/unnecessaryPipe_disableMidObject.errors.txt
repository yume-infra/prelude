=== Metadata ===
Effect version: 3.19.19

/.src/unnecessaryPipe_disableMidObject.ts(6,6): suggestion TS377013: This pipe call contains no arguments. effect(unnecessaryPipe)
/.src/unnecessaryPipe_disableMidObject.ts(9,6): suggestion TS377013: This pipe call contains no arguments. effect(unnecessaryPipe)


==== /.src/unnecessaryPipe_disableMidObject.ts (2 errors) ====
    // @effect-v3
    import { pipe } from "effect"
    import * as Effect from "effect/Effect"
    
    export const test = {
      a: pipe(Effect.succeed(42)),
         ~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377013: This pipe call contains no arguments. effect(unnecessaryPipe)
      // @effect-diagnostics-next-line unnecessaryPipe:off
      b: pipe(Effect.succeed(43)),
      c: pipe(Effect.succeed(44))
         ~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377013: This pipe call contains no arguments. effect(unnecessaryPipe)
    }
    
