=== Metadata ===
Effect version: 3.19.19

/.src/effectFnOpportunity_params.ts(7,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(a, b) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_params.ts(14,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(a, b) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_params.ts(21,17): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(a, b) { ... })`. effect(effectFnOpportunity)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["span", "suggested-span", "inferred-span", "no-span", "untraced"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_params.ts (3 errors) ====
    // @effect-v3
    import * as Effect from "effect/Effect"
    
    // These cases are okay and should trigger the diagnostic.
    // Functions with parameters can be converted to Effect.fn.
    
    export const arrowWithParams = (a: number, b: string) => {
                 ~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(a, b) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(a)
        return b
      })
    }
    
    export const functionExpressionWithParams = function(a: number, b: string) {
                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(a, b) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(a)
        return b
      })
    }
    
    export function functionDeclarationWithParams(a: number, b: string) {
                    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(a, b) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(a)
        return b
      })
    }
    
