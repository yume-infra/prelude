=== Metadata ===
Effect version: 3.19.19

/.src/globalFetch.ts(5,22): warning TS377061: This code uses the global `fetch` function, HTTP requests are represented through `HttpClient` from `@effect/platform`. effect(globalFetch)
/.src/globalFetch.ts(8,29): warning TS377061: This code uses the global `fetch` function, HTTP requests are represented through `HttpClient` from `@effect/platform`. effect(globalFetch)
/.src/globalFetch.ts(12,23): warning TS377061: This code uses the global `fetch` function, HTTP requests are represented through `HttpClient` from `@effect/platform`. effect(globalFetch)
/.src/globalFetch.ts(21,20): warning TS377061: This code uses the global `fetch` function, HTTP requests are represented through `HttpClient` from `@effect/platform`. effect(globalFetch)


==== /.src/globalFetch.ts (4 errors) ====
    // @effect-diagnostics globalFetch:warning
    import { Effect } from "effect"
    
    // Should trigger - fetch at module level
    const _moduleFetch = fetch("https://example.com/module")
                         ~~~~~
!!! warning TS377061: This code uses the global `fetch` function, HTTP requests are represented through `HttpClient` from `@effect/platform`. effect(globalFetch)
    
    // Should trigger - fetch in regular function
    const _regularFetch = () => fetch("https://example.com/regular")
                                ~~~~~
!!! warning TS377061: This code uses the global `fetch` function, HTTP requests are represented through `HttpClient` from `@effect/platform`. effect(globalFetch)
    
    // Should trigger - aliased fetch
    const myFetch = fetch
    const _aliasedFetch = myFetch("https://example.com/alias")
                          ~~~~~~~
!!! warning TS377061: This code uses the global `fetch` function, HTTP requests are represented through `HttpClient` from `@effect/platform`. effect(globalFetch)
    
    // Should NOT trigger - fetch directly inside Effect.gen
    export const fetchInGen = Effect.gen(function*() {
      fetch("https://example.com/in-gen")
    })
    
    // Should trigger - fetch inside nested arrow in generator
    export const nestedArrowInGen = Effect.gen(function*() {
      const fn = () => fetch("https://example.com/nested")
                       ~~~~~
!!! warning TS377061: This code uses the global `fetch` function, HTTP requests are represented through `HttpClient` from `@effect/platform`. effect(globalFetch)
      return fn
    })
    
