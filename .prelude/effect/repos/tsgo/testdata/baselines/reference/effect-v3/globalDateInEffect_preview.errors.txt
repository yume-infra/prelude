=== Metadata ===
Effect version: 3.19.19

/.src/globalDateInEffect_preview.ts(6,15): warning TS377067: This Effect code uses `Date.now()`, time access in Effect code is represented through `Clock` from Effect. effect(globalDateInEffect)


==== /.src/globalDateInEffect_preview.ts (1 errors) ====
    // @effect-diagnostics *:off
    // @effect-diagnostics globalDateInEffect:warning
    import { Effect } from "effect"
    
    export const preview = Effect.gen(function*() {
      const now = Date.now()
                  ~~~~~~~~~~
!!! warning TS377067: This Effect code uses `Date.now()`, time access in Effect code is represented through `Clock` from Effect. effect(globalDateInEffect)
      return now
    })
    
