=== Metadata ===
Effect version: 3.19.19

/.src/effectFnOpportunity_inferred.ts(11,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("shouldHaveSuggestion")(function*() { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_inferred.ts(18,17): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("shouldHaveSuggestionFunction")(function*() { ... })`. effect(effectFnOpportunity)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["inferred-span"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_inferred.ts (2 errors) ====
    // @effect-v3
    import * as Effect from "effect/Effect"
    
    const _notExportedNoSuggestion = () => {
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return 42
      })
    }
    
    export const shouldHaveSuggestion = () => {
                 ~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("shouldHaveSuggestion")(function*() { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return 42
      })
    }
    
    export function shouldHaveSuggestionFunction() {
                    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("shouldHaveSuggestionFunction")(function*() { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return 42
      })
    }
    
