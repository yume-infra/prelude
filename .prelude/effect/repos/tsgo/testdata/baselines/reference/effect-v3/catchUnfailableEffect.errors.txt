=== Metadata ===
Effect version: 3.19.19

/.src/catchUnfailableEffect.ts(7,3): suggestion TS377009: The previous Effect does not fail, so this error-handling branch will never run. effect(catchUnfailableEffect)
/.src/catchUnfailableEffect.ts(16,3): suggestion TS377009: The previous Effect does not fail, so this error-handling branch will never run. effect(catchUnfailableEffect)
/.src/catchUnfailableEffect.ts(38,3): suggestion TS377009: The previous Effect does not fail, so this error-handling branch will never run. effect(catchUnfailableEffect)
/.src/catchUnfailableEffect.ts(41,38): suggestion TS377009: The previous Effect does not fail, so this error-handling branch will never run. effect(catchUnfailableEffect)


==== /.src/catchUnfailableEffect.ts (4 errors) ====
    // @effect-v3
    // @effect-diagnostics catchToOrElseSucceed:off
    import * as Effect from "effect/Effect"
    import { pipe } from "effect/Function"
    
    export const shouldReport = Effect.succeed(42).pipe(
      Effect.catchAll(() => Effect.void) // <- should report here
      ~~~~~~~~~~~~~~~
!!! suggestion TS377009: The previous Effect does not fail, so this error-handling branch will never run. effect(catchUnfailableEffect)
    )
    
    export const shouldNotReport = Effect.fail("error").pipe(
      Effect.catchAll(() => Effect.succeed(42))
    )
    
    export const shouldReportPipe = pipe(
      Effect.succeed(42),
      Effect.catchAll(() => Effect.void) // <- should report here
      ~~~~~~~~~~~~~~~
!!! suggestion TS377009: The previous Effect does not fail, so this error-handling branch will never run. effect(catchUnfailableEffect)
    )
    
    export const shouldNotReportPipe = pipe(
      Effect.fail("error"),
      Effect.catchAll(() => Effect.succeed(42)) // <- should not report here
    )
    
    export const shouldTriggerThirdArg = Effect.succeed(42).pipe(
      Effect.flatMap(() => Effect.fail("error")),
      Effect.catchAll(() => Effect.succeed(42)) // <- should not report here
    )
    
    export const shouldNotTriggerThirdArgPipe = pipe(
      Effect.succeed(42),
      Effect.flatMap(() => Effect.fail("error")),
      Effect.catchAll(() => Effect.succeed(42)) // <- should not report here
    )
    
    export const shouldTriggerThirdArgPipe = pipe(
      Effect.succeed(42),
      Effect.flatMap(() => Effect.void),
      Effect.catchAll(() => Effect.succeed(42)) // <- should report here
      ~~~~~~~~~~~~~~~
!!! suggestion TS377009: The previous Effect does not fail, so this error-handling branch will never run. effect(catchUnfailableEffect)
    )
    
    export const shouldReportDataFirst = Effect.catchAll(
                                         ~~~~~~~~~~~~~~~
!!! suggestion TS377009: The previous Effect does not fail, so this error-handling branch will never run. effect(catchUnfailableEffect)
      Effect.never,
      () => Effect.log("error") // <- should report here
    )
    
