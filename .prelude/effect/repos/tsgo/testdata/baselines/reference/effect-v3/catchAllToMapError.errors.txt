=== Metadata ===
Effect version: 3.19.19

/.src/catchAllToMapError.ts(18,3): suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
/.src/catchAllToMapError.ts(23,3): suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
/.src/catchAllToMapError.ts(31,3): suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
/.src/catchAllToMapError.ts(36,3): suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
/.src/catchAllToMapError.ts(65,3): suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)


==== /.src/catchAllToMapError.ts (5 errors) ====
    // @effect-v3
    // @effect-diagnostics catchToOrElseSucceed:off
    import * as Effect from "effect/Effect"
    import { pipe } from "effect/Function"
    
    class MyErrorTagged {
      readonly _tag = "MyErrorTagged"
      constructor(readonly cause: unknown) {}
    }
    
    class AnotherError {
      readonly _tag = "AnotherError"
      constructor(readonly message: string) {}
    }
    
    // Should trigger: catchAll with arrow function returning Effect.fail
    export const shouldTriggerArrow = Effect.fail("error").pipe(
      Effect.catchAll((cause) => Effect.fail(new MyErrorTagged(cause)))
      ~~~~~~~~~~~~~~~
!!! suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
    )
    
    // Should trigger: catchAll with arrow function returning Effect.fail (block body)
    export const shouldTriggerArrowBlock = Effect.fail("error").pipe(
      Effect.catchAll((cause) => {
      ~~~~~~~~~~~~~~~
!!! suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
        return Effect.fail(new MyErrorTagged(cause))
      })
    )
    
    // Should trigger: pipe style
    export const shouldTriggerPipe = pipe(
      Effect.fail("error"),
      Effect.catchAll((cause) => Effect.fail(new MyErrorTagged(cause)))
      ~~~~~~~~~~~~~~~
!!! suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
    )
    
    // Should trigger: with function expression
    export const shouldTriggerFunctionExpr = Effect.fail("error").pipe(
      Effect.catchAll(function(cause) {
      ~~~~~~~~~~~~~~~
!!! suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
        return Effect.fail(new MyErrorTagged(cause))
      })
    )
    
    // Should NOT trigger: catchAll with logic other than just Effect.fail
    export const shouldNotTriggerWithLogic = Effect.fail("error").pipe(
      Effect.catchAll((cause) => {
        console.log(cause)
        return Effect.fail(new MyErrorTagged(cause))
      })
    )
    
    // Should NOT trigger: catchAll returning Effect.succeed
    export const shouldNotTriggerSucceed = Effect.fail("error").pipe(
      Effect.catchAll(() => Effect.succeed(42))
    )
    
    // Should NOT trigger: catchAll with Effect.flatMap or other operations
    export const shouldNotTriggerChained = Effect.fail("error").pipe(
      Effect.catchAll((cause) =>
        Effect.fail(new MyErrorTagged(cause)).pipe(
          Effect.tap(() => Effect.void)
        )
      )
    )
    
    // Should trigger: simple error wrapping
    export const shouldTriggerSimple = Effect.fail("original").pipe(
      Effect.catchAll((e) => Effect.fail(new AnotherError(String(e))))
      ~~~~~~~~~~~~~~~
!!! suggestion TS377010: `Effect.mapError` expresses the same error-type transformation more directly than `Effect.catchAll` followed by `Effect.fail`. effect(catchAllToMapError)
    )
    
