=== Metadata ===
Effect version: 4.0.0

/.src/effectFnOpportunity_noPipeWithSpan.ts(10,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_noPipeWithSpan.ts(11,3): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_noPipeWithSpan.ts(15,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_noPipeWithSpan.ts(16,10): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_noPipeWithSpan.ts(21,17): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_noPipeWithSpan.ts(22,10): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["span", "suggested-span", "inferred-span", "no-span", "untraced"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_noPipeWithSpan.ts (6 errors) ====
    import * as Effect from "effect/Effect"
    
    // This case differs from pipeWithSpan because the withSpan is not the last
    // piped transformation, so we cannot extract the span name to use with
    // Effect.fn. Instead, we should suggest Effect.fnUntraced for these cases.
    // Note: unnecessaryEffectGen diagnostics ARE expected for the inner Effect.gen
    // calls (each has a single-statement body). The Go test harness runs all rules,
    // unlike upstream which runs only the rule matching the file name prefix.
    
    export const arrowWithPipe = (n: number) =>
                 ~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      Effect.gen(function*() {
      ~~~~~~~~~~~~~~~~~~~~~~~~
        return yield* Effect.succeed(n)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe(Effect.ignore)
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    
    export const functionExpressionWithPipe = function(n: number) {
                 ~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
             ~~~~~~~~~~~~~~~~~~~~~~~~
        return yield* Effect.succeed(n)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe(Effect.withSpan("functionExpressionWithPipe"), Effect.ignore)
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    }
    
    export function functionDeclarationWithPipe(n: number) {
                    ~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
             ~~~~~~~~~~~~~~~~~~~~~~~~
        return yield* Effect.succeed(n)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe(Effect.map((x) => x + 1), Effect.withSpan("functionDeclarationWithPipe"), Effect.ignore)
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    }
    
