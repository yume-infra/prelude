=== Metadata ===
Effect version: 4.0.0

/.src/newSchemaClass.ts(12,28): suggestion TS377094: This Schema class is constructed with `new`. `User.make(...)` can be used to construct the Schema class instance. effect(newSchemaClass)
/.src/newSchemaClass.ts(15,14): suggestion TS377094: This Schema class is constructed with `new`. `User.make(...)` can be used to construct the Schema class instance. effect(newSchemaClass)
/.src/newSchemaClass.ts(19,30): suggestion TS377094: This Schema class is constructed with `new`. `User.make(...)` can be used to construct the Schema class instance. effect(newSchemaClass)


==== /.src/newSchemaClass.ts (3 errors) ====
    // @effect-diagnostics newSchemaClass:suggestion
    import { Effect, Schema } from "effect"
    
    class User extends Schema.Class<User>("User")({
      name: Schema.String
    }) {}
    
    const PlainSchema = Schema.Struct({
      name: Schema.String
    })
    
    export const newUser = new User({ name: "John" })
                               ~~~~
!!! suggestion TS377094: This Schema class is constructed with `new`. `User.make(...)` can be used to construct the Schema class instance. effect(newSchemaClass)
    
    export const newInsideEffect = Effect.gen(function*() {
      return new User({ name: "Jane" })
                 ~~~~
!!! suggestion TS377094: This Schema class is constructed with `new`. `User.make(...)` can be used to construct the Schema class instance. effect(newSchemaClass)
    })
    
    export const nestedFunction = Effect.gen(function*() {
      const makeUser = () => new User({ name: "Nested" })
                                 ~~~~
!!! suggestion TS377094: This Schema class is constructed with `new`. `User.make(...)` can be used to construct the Schema class instance. effect(newSchemaClass)
      return makeUser
    })
    
    export const plainSchemaInGen = Effect.gen(function*() {
      const user = PlainSchema.make({ name: "Struct" })
      return user
    })
    
    class RegularUser {
      constructor(readonly name: string) {}
    }
    
    export const regular = new RegularUser("Regular")
    
