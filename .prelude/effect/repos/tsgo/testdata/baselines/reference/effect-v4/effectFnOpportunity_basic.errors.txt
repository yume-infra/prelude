=== Metadata ===
Effect version: 4.0.0

/.src/effectFnOpportunity_basic.ts(9,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_basic.ts(16,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_basic.ts(22,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_basic.ts(29,17): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["span", "suggested-span", "inferred-span", "no-span", "untraced"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_basic.ts (4 errors) ====
    import * as Effect from "effect/Effect"
    
    // These cases can be converted to Effect.fnUntraced because:
    // - There are no pipe arguments
    // - There is no withSpan for tracing
    // Converting to Effect.fnUntraced improves performance by not
    // reallocating the generator function on every call.
    
    export const arrowBlockGen = (n: number) => {
                 ~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return n
      })
    }
    
    export const arrowExpressionGen = (n: number) =>
                 ~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
      Effect.gen(function*() {
        yield* Effect.succeed(1)
        return n
      })
    
    export const functionExpressionGen = function(n: number) {
                 ~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return n
      })
    }
    
    export function functionDeclarationGen(n: number) {
                    ~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
        yield* Effect.succeed(1)
        return n
      })
    }
    
