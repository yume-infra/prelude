=== Metadata ===
Effect version: 4.0.0

/.src/effectFnOpportunity_inferredServiceMake.ts(6,13): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("UserService.getUser")(function*(id) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_inferredServiceMake.ts(7,17): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["inferred-span"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_inferredServiceMake.ts (2 errors) ====
    import { Effect, Layer, Context } from "effect"
    
    export class UserService extends Context.Service<UserService>()("UserService", {
        make: Effect.gen(function*() {
            return {
                getUser: (id: string) =>
                ~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("UserService.getUser")(function*(id) { ... })`. effect(effectFnOpportunity)
                    Effect.gen(function*() {
                    ~~~~~~~~~~~~~~~~~~~~~~~~
                        yield* Effect.log(`Looking up user ${id}`)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
                    })
    ~~~~~~~~~~~~~~~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
            }
        })
    }) {}
    
