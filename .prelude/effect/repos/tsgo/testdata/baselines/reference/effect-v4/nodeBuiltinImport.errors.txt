=== Metadata ===
Effect version: 4.0.0

/.src/nodeBuiltinImport.ts(5,16): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(6,22): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(7,26): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(8,39): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(9,22): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(10,18): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(11,31): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(12,31): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(13,31): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(14,31): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(15,22): warning TS377057: This module reference uses the `child_process` module, the corresponding Effect API is `ChildProcess` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(16,23): warning TS377057: This module reference uses the `child_process` module, the corresponding Effect API is `ChildProcess` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(17,18): warning TS377057: This module reference uses the `http` module, the corresponding Effect API is `HttpClient` from `effect/unstable/http`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(18,19): warning TS377057: This module reference uses the `https` module, the corresponding Effect API is `HttpClient` from `effect/unstable/http`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(21,8): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(24,21): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "types": ["node"],
        "plugins": [
          {
            "name": "@effect/language-service",
            "ignoreEffectErrorsInTscExitCode": true,
            "skipDisabledOptimization": true
          }
        ]
      }
    }
    

==== /.src/nodeBuiltinImport.ts (16 errors) ====
    // @effect-diagnostics nodeBuiltinImport:warning
    import { pipe } from "effect"
    
    // Flagged: ES module imports for covered modules
    import fs from "fs"
                   ~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    import * as fs2 from "node:fs"
                         ~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    import { readFile } from "fs/promises"
                             ~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    import { readFile as readFile2 } from "node:fs/promises"
                                          ~~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    import { join } from "path"
                         ~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import path from "node:path"
                     ~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { join as join2 } from "path/posix"
                                  ~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { join as join3 } from "node:path/posix"
                                  ~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { join as join4 } from "path/win32"
                                  ~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { join as join5 } from "node:path/win32"
                                  ~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { exec } from "child_process"
                         ~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `child_process` module, the corresponding Effect API is `ChildProcess` from `effect`. effect(nodeBuiltinImport)
    import { spawn } from "node:child_process"
                          ~~~~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `child_process` module, the corresponding Effect API is `ChildProcess` from `effect`. effect(nodeBuiltinImport)
    import http from "http"
                     ~~~~~~
!!! warning TS377057: This module reference uses the `http` module, the corresponding Effect API is `HttpClient` from `effect/unstable/http`. effect(nodeBuiltinImport)
    import https from "node:https"
                      ~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `https` module, the corresponding Effect API is `HttpClient` from `effect/unstable/http`. effect(nodeBuiltinImport)
    
    // Flagged: side-effect import
    import "node:fs"
           ~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    
    // Flagged: top-level require
    const fs3 = require("node:fs")
                        ~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    
    // Not flagged: Effect-native imports
    // @ts-expect-error - @effect/platform not installed in harness
    import { FileSystem } from "@effect/platform"
    
    // Not flagged: third-party modules with similar names
    // @ts-expect-error - fs-extra not installed in harness
    import fsExtra from "fs-extra"
    
