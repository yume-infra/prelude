=== Metadata ===
Effect version: 4.0.0

/.src/globalTimers.ts(5,18): warning TS377072: This code uses `setTimeout`, the corresponding Effect timer API is `Effect.sleep or Schedule` from Effect. effect(globalTimers)
/.src/globalTimers.ts(8,25): warning TS377072: This code uses `setInterval`, the corresponding Effect timer API is `Schedule or Effect.repeat` from Effect. effect(globalTimers)
/.src/globalTimers.ts(12,25): warning TS377072: This code uses `setTimeout`, the corresponding Effect timer API is `Effect.sleep or Schedule` from Effect. effect(globalTimers)
/.src/globalTimers.ts(21,20): warning TS377072: This code uses `setInterval`, the corresponding Effect timer API is `Schedule or Effect.repeat` from Effect. effect(globalTimers)


==== /.src/globalTimers.ts (4 errors) ====
    // @effect-diagnostics globalTimers:warning
    import { Effect } from "effect"
    
    // Should trigger - setTimeout at module level
    const _timeout = setTimeout(() => {}, 100)
                     ~~~~~~~~~~~~~~~~~~~~~~~~~
!!! warning TS377072: This code uses `setTimeout`, the corresponding Effect timer API is `Effect.sleep or Schedule` from Effect. effect(globalTimers)
    
    // Should trigger - setInterval in regular function
    const _interval = () => setInterval(() => {}, 100)
                            ~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! warning TS377072: This code uses `setInterval`, the corresponding Effect timer API is `Schedule or Effect.repeat` from Effect. effect(globalTimers)
    
    // Should trigger - aliased setTimeout at module level
    const later = setTimeout
    const _aliasedTimeout = later(() => {}, 100)
                            ~~~~~~~~~~~~~~~~~~~~
!!! warning TS377072: This code uses `setTimeout`, the corresponding Effect timer API is `Effect.sleep or Schedule` from Effect. effect(globalTimers)
    
    // Should NOT trigger - setTimeout inside Effect.gen
    export const setTimeoutInGen = Effect.gen(function*() {
      return setTimeout(() => {}, 100)
    })
    
    // Should trigger - setInterval inside nested arrow in generator
    export const nestedArrowInGen = Effect.gen(function*() {
      const fn = () => setInterval(() => {}, 100)
                       ~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! warning TS377072: This code uses `setInterval`, the corresponding Effect timer API is `Schedule or Effect.repeat` from Effect. effect(globalTimers)
      return fn
    })
    
    // Should NOT trigger - shadowed setTimeout
    const _shadowedTimeout = () => {
      const setTimeout = () => 0
      return setTimeout()
    }
    
