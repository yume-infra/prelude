=== Metadata ===
Effect version: 4.0.0

/.src/effectFnOpportunity_typeParams.ts(11,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*<T>(value) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_typeParams.ts(12,10): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_typeParams.ts(17,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*<T>(value) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_typeParams.ts(18,10): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_typeParams.ts(23,17): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*<T>(value) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_typeParams.ts(24,10): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_typeParams.ts(32,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn(<T>(value) => { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_typeParams.ts(41,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn(function<T>(value) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_typeParams.ts(50,17): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn(function<T>(value) { ... })`. effect(effectFnOpportunity)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["span", "suggested-span", "inferred-span", "no-span", "untraced"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_typeParams.ts (9 errors) ====
    import * as Effect from "effect/Effect"
    
    // Note: Known formatting divergences from upstream (TypeScript-Go printer artifacts):
    // - Semicolons added to body statements inside replaced functions (e.g. `const a = 1;` vs `const a = 1`)
    // - Space before parens in zero-parameter function expressions (`function ()` vs `function()`)
    // - Trailing semicolons on replacement statements (`});` vs `})`)
    
    // The diagnostic should trigger for these cases. We should check that
    // the quick fixes have kept the generic type arguments.
    
    export const arrowWithTypeParams = <T>(value: T) => {
                 ~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*<T>(value) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
             ~~~~~~~~~~~~~~~~~~~~~~~~
        return yield* Effect.succeed(value)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      })
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    }
    
    export const functionExpressionWithTypeParams = function<T>(value: T) {
                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*<T>(value) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
             ~~~~~~~~~~~~~~~~~~~~~~~~
        return yield* Effect.succeed(value)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      })
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    }
    
    export function functionDeclarationWithTypeParams<T>(value: T) {
                    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*<T>(value) { ... })`. effect(effectFnOpportunity)
      return Effect.gen(function*() {
             ~~~~~~~~~~~~~~~~~~~~~~~~
        return yield* Effect.succeed(value)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      })
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    }
    
    // Regular functions with type parameters returning Effect (no Effect.gen)
    // These have >5 statements so they should trigger the diagnostic
    
    export const arrowWithTypeParamsNoGen = <T>(value: T) => {
                 ~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn(<T>(value) => { ... })`. effect(effectFnOpportunity)
      const _a = 1
      const _b = 2
      const _c = 3
      const _d = 4
      const _e = 5
      return Effect.succeed(value)
    }
    
    export const functionExpressionWithTypeParamsNoGen = function<T>(value: T) {
                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn(function<T>(value) { ... })`. effect(effectFnOpportunity)
      const _a = 1
      const _b = 2
      const _c = 3
      const _d = 4
      const _e = 5
      return Effect.succeed(value)
    }
    
    export function functionDeclarationWithTypeParamsNoGen<T>(value: T) {
                    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn(function<T>(value) { ... })`. effect(effectFnOpportunity)
      const _a = 1
      const _b = 2
      const _c = 3
      const _d = 4
      const _e = 5
      return Effect.succeed(value)
    }
    
