=== Metadata ===
Effect version: 4.0.0

/.src/effectFnOpportunity_inferredOf.ts(9,3): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("UserService.getUser")(function*(id) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_inferredOf.ts(10,5): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["inferred-span"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_inferredOf.ts (2 errors) ====
    import { Effect, Layer, Context } from "effect"
    
    class UserService extends Context.Service<UserService, {
        getUser(id: string): Effect.Effect<void>
      }>()("UserService") {}
    
    
    const _shouldTrigger = UserService.of({ // UserService is an Effect Tag
      getUser: (id: string) =>
      ~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("UserService.getUser")(function*(id) { ... })`. effect(effectFnOpportunity)
        Effect.gen(function*() {
        ~~~~~~~~~~~~~~~~~~~~~~~~
          yield* Effect.log(`Looking up user ${id}`)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
        })
    ~~~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    })
    
