{{#if @config.stateManagement}}
import { createPinia } from 'pinia'
{{/if}}
import { createApp } from 'vue'
import App from './App.vue'
{{#if @config.router}}
import router from './router'
{{/if}}
{{> global/import-root-css }}

createApp(App)
{{#if @config.stateManagement}}
  .use(createPinia())
{{/if}}
{{#if @config.router}}
  .use(router)
{{/if}}
  .mount('#root')
