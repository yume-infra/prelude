# {{@config.name}}

TypeScript ESM Node project generated by @sayoriqwq/prelude.

## Scripts

```bash
pnpm install
pnpm build
pnpm start
```

The build output is written to `dist/index.js`, with declarations at `dist/index.d.ts`.
