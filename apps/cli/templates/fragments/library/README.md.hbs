# {{@config.name}}

TypeScript ESM library package generated by create-yume.

## Scripts

```bash
pnpm install
pnpm build
```

The build output is written to `dist/index.js`, with declarations at `dist/index.d.ts`.
