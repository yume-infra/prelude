# {{@config.name}}

TypeScript ESM CLI tool generated by @sayoriqwq/prelude.

## Scripts

```bash
pnpm install
pnpm build
pnpm smoke:bin
```

The build output is written to `dist/index.js`, with declarations at `dist/index.d.ts`.
The package exposes the `{{@config.name}}` npm bin at `dist/index.js`.
